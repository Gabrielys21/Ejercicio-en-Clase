using System;
class Program
{
    static void Main()
    {
        int Sum = 0;
            for (int Num = 1; Num < 11; Num++)
        {
            Sum += Num;
        }
            Console.WriteLine(Sum);
    }
}